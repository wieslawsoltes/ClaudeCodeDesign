{
  "name": "claude-code-design",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An independent local-first AI coding workspace with a progressive WebGPU interface.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
